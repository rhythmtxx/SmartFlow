# 前端工程化（React + Vite 迁移）任务大纲

> **任务交接文档**：本大纲供接手此任务的开发者使用。实现前请先阅读文末「前置阅读」列出的代码文件。
> 关联 Roadmap：README「Roadmap」→ 前端工程化（迁移到 React/Vite，组件化 + 测试）。

---

## 0. 任务说明

| 项 | 内容 |
|---|---|
| 任务 | 把当前单文件前端（`static/index.html`，约 1100 行）迁移为 React + Vite 工程化项目 |
| 目标 | 组件化、类型化、可测试、可维护；**功能与视觉 1:1 等价**，后端接口零改动 |
| 预计工作量 | 2~3 天（渐进迁移，非大爆炸重写） |
| 面向岗位 | FDE（前端工程能力展示）为主，AI 应用开发岗也能体现工程化素养 |

**验收一句话**：新前端由 Vite 构建、FastAPI 伺服，12 个核心交互区功能与视觉等价（SSE 流式、HITL 审批、会话管理、鉴权、可观测性、文件管理）；Vitest 单元测试通过；后端 `/api/*` 接口零改动。

---

## 1. 现状与目标

### 1.1 现状（要替换的东西）

- 单文件 `static/index.html`（约 1100 行）：HTML + 内联 CSS + 内联 JS 混在一起
- CDN 依赖：Tailwind（`cdn.tailwindcss.com`）、marked.js、Google Fonts——**离线/内网不可用**，且无版本锁定
- JS 用原生 DOM 操作 + 全局函数（`onclick="..."`），无类型、无测试、无构建
- 核心交互区（迁移时必须 1:1 保留，**12 个**）：

| # | 元素 | 功能 |
|---|---|---|
| 1 | `chat-form` / `chat-container` | 消息发送 + SSE 流式渲染（markdown） |
| 2 | `approval-overlay` | HITL 审批弹窗（60s 倒计时、同意/拒绝） |
| 3 | `sessions-list` | 会话列表（新建/切换/删除，localStorage 记忆当前会话） |
| 4 | `api-token-input` | API Token 配置（localStorage + 统一鉴权头） |
| 5 | `token-prompt` / `token-completion` / `token-total` | Token 数字仪表 |
| 6 | `cost-estimate` | 成本估算显示 |
| 7 | `tool-calls-list` | 工具调用时间线（按风险着色、失败标 ✗） |
| 8 | `skills-list` / `tools-list` | 技能与工具列表（轮询刷新） |
| 9 | `memory-content` | 长期记忆面板（markdown 渲染） |
| 10 | `outputs-list` + `file-upload` | 输出文件列表（打开=带鉴权下载 / 删除 / 上传） |
| 11 | `chat-input` + 发送按钮 | 输入交互（生成中禁用） |
| 12 | 轮询定时器 | status 10s / outputs 5s / memory 10s / sessions 30s / stats 3s |

### 1.2 目标

- React 18 + Vite + TypeScript 工程化结构
- 组件化拆分 + 状态管理 + 自定义 hooks
- Vitest 单元测试 + ESLint/Prettier 质量工具
- 构建产物由 FastAPI 伺服（后端只改伺服部分，`/api/*` 零改动）

---

## 2. 技术选型与理由

| 项 | 选型 | 理由 |
|---|---|---|
| 构建工具 | **Vite 5** | 启动快、TS 开箱即用、生态主流 |
| 框架 | **React 18** | 求职市场主流；FDE 岗必考 |
| 语言 | **TypeScript** | 类型化是工程化核心；与后端 API 契约（types.ts）对应 |
| 样式 | **Tailwind（npm 包 + PostCSS 构建）** | **保留现有全部视觉**（赛博/HUD 风格），仅从 CDN 迁到本地构建，样式类基本不用改 |
| Markdown | **react-markdown**（替换 marked） | React 组件化渲染；保留现有 prose 样式 |
| 状态管理 | **Zustand** | 轻量（~1KB）、无样板；适合"会话/Token/消息/统计"这类全局状态 |
| 测试 | **Vitest + @testing-library/react** | 组件/单元测试；与 Vite 同生态 |
| 质量 | **ESLint + Prettier** | 规范提交质量 |
| 后端伺服 | FastAPI `StaticFiles(html=True)` | 见 4.6 |

> 备选（可降级）：状态管理用 React Context + hooks（不引 Zustand）；测试只做核心组件（聊天流式、审批弹窗）。降级不影响验收主项。

---

## 3. 目录结构与组件拆分

### 3.1 目录（建议 `frontend/`，与 `static/` 并存直至迁移完成）

```
frontend/
├── package.json
├── vite.config.ts          # 含 dev proxy: /api -> http://localhost:8000
├── tsconfig.json
├── index.html              # Vite 入口（title: SmartFlow）
└── src/
    ├── main.tsx
    ├── App.tsx             # 布局：Sidebar + ChatArea
    ├── api/
    │   ├── client.ts       # fetch 封装：统一 baseURL + Authorization 头 + 401 处理
    │   └── types.ts        # 与后端 API 契约（ChatRequest/Session/Stats/Output...）
    ├── store/
    │   ├── useAuthStore.ts     # apiToken（localStorage 持久化）
    │   ├── useSessionStore.ts  # currentSession + sessions 列表
    │   └── useChatStore.ts     # messages / tokens / isGenerating
    ├── hooks/
    │   ├── useChatStream.ts    # SSE 流式解析（fetch + ReadableStream）
    │   ├── usePolling.ts       # 通用轮询 hook（interval + cleanup）
    │   └── useStats.ts         # /api/stats 轮询
    ├── components/
    │   ├── layout/Sidebar.tsx
    │   ├── sidebar/SessionsPanel.tsx
    │   ├── sidebar/TelemetryPanel.tsx      # TokenMeter + CostEstimate + ToolCallList
    │   ├── sidebar/ApiTokenInput.tsx
    │   ├── sidebar/SkillsList.tsx / ToolsList.tsx / MemoryPanel.tsx / OutputsPanel.tsx
    │   ├── chat/ChatContainer.tsx / MessageBubble.tsx / ToolCallCard.tsx / ChatInput.tsx
    │   └── dialogs/ApprovalDialog.tsx       # 60s 倒计时
    ├── styles/index.css     # Tailwind 指令 + 现有自定义样式迁移
    └── test/                # 测试辅助（mock fetch / 渲染工具）
```

### 3.2 组件职责要点

- **MessageBubble**：按 role（user/assistant/system/tool）渲染；assistant 用 react-markdown
- **ToolCallCard**：工具调用内联展示（名称、参数 JSON、结果摘要、风险着色、✓/✗）
- **ApprovalDialog**：接收 approval 事件 → 展示工具/参数 → 60s 倒计时 → 提交 `/api/approve`（带 session）
- **OutputsPanel**：列表 + 上传（FormData + 鉴权头）+ 打开（fetch Blob → objectURL → 新窗口/下载）+ 删除
- **useChatStream**：核心 hook，负责：POST `/api/chat`（SSE）→ 解析 `data:` 事件 → 按类型派发（text_delta / tool_call_* / approval_* / token_usage / error）

---

## 4. 关键实现方案

### 4.1 SSE 流式（useChatStream）

- 沿用现有实现方式：`fetch` + `ReadableStream`（**不是 EventSource**，因为要带 Authorization 头 + POST body）
- 解析逻辑封装进 hook：`streamBuffer` 按 `\n\n` 切分、`data: ` 前缀剥离、`JSON.parse`
- 事件类型 → store 更新映射表（与后端 `loop.py` 事件一一对应）

### 4.2 鉴权（useAuthStore + client.ts）

- `apiToken` 存 localStorage（key 沿用 `smartflow_api_token`，与现有一致，兼容存量用户）
- `client.ts` 统一：`headers: { Authorization: Bearer xxx }`（有 token 才加）
- 401 统一处理：提示 + 引导去侧栏配置

### 4.3 会话管理（useSessionStore）

- `currentSession` 存 localStorage（key 沿用 `smartflow_session_id`）
- 新建/切换/删除逻辑与现有 JS 等价：切换时清空消息区、重置 token 数字、并行刷新 5 个面板
- 审批提交带当前 session

### 4.4 轮询（usePolling）

- 统一 hook：`usePolling(fn, interval)`，组件卸载自动清理
- 频率沿用：status 10s / outputs 5s / memory 10s / sessions 30s / stats 3s

### 4.5 类型契约（api/types.ts）

- 从后端响应反向定义：`SessionInfo`、`ChatEvent`（判别联合：text_delta | tool_call_start | ...）、`StatsResponse`、`OutputFile` 等——这是 TS 化最大的收益点，SSE 事件用 discriminated union 保证 `switch` 穷尽

### 4.6 后端伺服（app.py 最小改动）

```python
# 生产模式：伺服 Vite 构建产物（需先 npm run build）
import os
if os.path.exists("frontend/dist"):
    app.mount("/", StaticFiles(directory="frontend/dist", html=True), name="frontend")
```

- **注意顺序**：`/api/*` 路由与 `/outputs` 挂载必须在 StaticFiles `/` 之前定义（Starlette 路由按注册顺序匹配）
- 开发模式：`vite.config.ts` 配 `server.proxy = { '/api': 'http://localhost:8000', '/outputs': 'http://localhost:8000' }`，前后端分开起
- 迁移完成后：删除 `static/index.html` 与旧挂载（`app.mount("/static", ...)` 和根路由 `FileResponse("static/index.html")`）
- Dockerfile：多阶段构建（node 阶段 `npm ci && npm run build` → python 阶段 COPY dist）

---

## 5. 实施步骤（渐进迁移，每阶段可运行）

### 阶段 A：工程骨架（半天）
1. `frontend/` 初始化：`npm create vite@latest frontend -- --template react-ts`
2. 安装依赖：tailwindcss + postcss + autoprefixer、react-markdown、zustand、vitest、@testing-library/react、eslint、prettier
3. `index.html` 标题/字体；`styles/index.css` 迁入现有 Tailwind 配置（cyber 色板、动画 keyframes）与自定义样式（`.chat-bubble-ai`、`.hud-border`、滚动条等）
4. `vite.config.ts`：dev proxy
5. 验证：`npm run build` 成功；后端能伺服 dist（临时空壳页面）

### 阶段 B：静态面板迁移（半天）
6. `api/types.ts` + `api/client.ts`
7. 侧栏静态组件：SkillsList / ToolsList / MemoryPanel / OutputsPanel（含上传/打开/删除）
8. `usePolling` hook 接入 status/outputs/memory 轮询
9. 验证：侧栏数据与旧前端一致

### 阶段 C：交互面板迁移（半天）
10. ApiTokenInput + useAuthStore（localStorage 兼容）
11. SessionsPanel + useSessionStore（新建/切换/删除 + loadSession 等价逻辑）
12. TelemetryPanel（token 数字 / cost / tool calls）+ useStats
13. 验证：会话切换、鉴权、统计与旧前端一致

### 阶段 D：聊天与审批（1 天，核心）
14. `useChatStream` + ChatContainer + MessageBubble（react-markdown）+ ToolCallCard
15. ApprovalDialog（60s 倒计时、提交带 session）
16. 历史恢复：挂载时按 session 拉 `/api/history` 渲染（含 tool 消息折叠展示）
17. 验证：与真实后端联调 SSE 全链路、审批弹窗、流式渲染

### 阶段 E：质量与收尾（半天）
18. Vitest 用例（见第 6 节）；ESLint + Prettier 配置
19. 切换伺服：app.py 挂载 dist，删除旧 `static/` 与旧挂载；Dockerfile 多阶段
20. 文档：README（前端开发/构建说明）、CHANGELOG、Roadmap 勾选

---

## 6. 测试计划

### 6.1 Vitest 单元/组件测试（建议至少覆盖）

| 用例 | 覆盖点 |
|---|---|
| `useChatStream` 解析 | mock fetch 返回 SSE 片段流 → 事件正确派发（text_delta 拼接、tool_call_start/end、token_usage） |
| `useChatStream` 边界 | 半个 `data:` 行跨 chunk、空行、非法 JSON 不崩溃 |
| `MessageBubble` | user/assistant/system 渲染；assistant markdown 渲染出 `<p>/<code>` |
| `ApprovalDialog` | 倒计时从 60 递减；同意/拒绝调用 `/api/approve` 且带 session |
| `client.ts` | 有 token 时带 Authorization 头；无 token 不带；401 触发统一提示 |
| `SessionsPanel` | 新建/切换调用对应 API；当前会话高亮 |
| `useSessionStore` | localStorage 读写、切会话重置 token 数字 |

### 6.2 手动/联调验证（等价性清单）

- [ ] SSE 流式逐字渲染，中文正常
- [ ] HITL：发"执行 echo hello" → 弹窗 → 同意执行 / 拒绝跳过 / 60s 超时自动拒
- [ ] 会话：新建→对话→切换→删除，各自记忆隔离
- [ ] 鉴权：配 Token 后所有请求带鉴权头；配错显示 401 提示
- [ ] 输出文件：上传→列表→打开（新窗口预览）→删除；开启鉴权后打开仍走带 token 下载
- [ ] 统计面板：token/成本/工具调用时间线随对话更新
- [ ] 刷新页面：会话、Token、历史恢复

### 6.3 回归（后端不动，应全绿）

- `python test_hitl.py`、`python test_compress.py`、`python eval/run_eval.py`、`python test_more_tools.py`

---

## 7. 验收清单（Done 定义）

- [ ] `frontend/` React+TS+Vite 工程；`npm run build` 产物可被 FastAPI 伺服
- [ ] 12 个核心交互区功能与视觉等价（按 6.2 逐项勾选）
- [ ] 组件拆分符合 3.1 目录；SSE 事件用 TS 判别联合类型
- [ ] Zustand store 覆盖会话/Token/消息/统计；localStorage key 与旧版兼容
- [ ] Vitest 核心用例通过；ESLint 无 error
- [ ] 开发模式（Vite proxy）与生产模式（FastAPI 伺服 dist）都能跑
- [ ] 旧 `static/index.html` 已移除、旧挂载已清理
- [ ] Dockerfile 多阶段构建成功
- [ ] 后端 `/api/*` 零改动（除伺服与旧挂载清理）
- [ ] README + CHANGELOG 更新、Roadmap 勾选

---

## 8. 注意事项 / 坑

1. **Starlette 挂载顺序**：`StaticFiles(directory="frontend/dist", html=True)` 挂 `/` 必须在所有 API 路由**之后**，否则 `/api/*` 会被静态目录吃掉（404）
2. **SSE 用 fetch 不是 EventSource**：EventSource 只能 GET 且带不了 Authorization 头——这正是现有实现选 fetch+ReadableStream 的原因，迁移时保持
3. **localStorage key 兼容**：`smartflow_api_token` / `smartflow_session_id` 沿用旧 key，用户升级不丢状态
4. **`/outputs` 静态直链已失效**：文件打开必须走 `/api/outputs/download/{name}`（带鉴权），React 里用 `fetch→Blob→objectURL`，不能直接 `<a href="/outputs/...">`
5. **CDN 依赖彻底移除**：Tailwind/marked/fonts 全部本地化，构建产物离线可用
6. **轮询竞态**：组件卸载后定时器必须清理（usePolling 内部处理）；切换会话时避免旧请求覆盖新状态（用 session 标识或 AbortController）
7. **不要大爆炸重写**：按阶段 A-E 渐进迁移，每阶段可运行可回退；旧 `static/` 在验收前保留
8. **Node 版本**：Docker 多阶段构建用 `node:20-alpine`；本地开发 `node >= 18`

---

## 9. 前置阅读

- `static/index.html`：**全部 JS**（SSE 解析、审批、会话、鉴权、输出文件逻辑的现成参考）+ 样式（cyber 色板、动画、chat-bubble 类）
- `app.py`：静态挂载、`/api/chat`（SSE 格式）、`/api/outputs/download`、鉴权中间件
- `core/loop.py`：事件类型清单（text_delta / tool_call_start / tool_call_end / approval_required / approval_resolved / token_usage / error / turn_end）
- `README.md`：API 接口表（前端要对接的全部接口）
