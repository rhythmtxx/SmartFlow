# 更多工具（Web 搜索 / HTTP 请求 / 代码沙箱）任务大纲

> **任务交接文档**：本大纲供接手此任务的开发者使用。实现前请先阅读文末「前置阅读」列出的代码文件。
> 关联 Roadmap：README「Roadmap」→ 更多工具（Web 搜索、HTTP 请求、代码执行沙箱）。

---

## 0. 任务说明

| 项 | 内容 |
|---|---|
| 任务 | 为 Agent 新增 3 个工具：`web_search`、`http_request`、`code_exec` |
| 目标 | 扩展 Agent 能力边界：联网检索、访问外部 API、在隔离沙箱中执行代码 |
| 预计工作量 | 1~1.5 天（三个工具 + 安全防护 + 测试） |
| 安全基调 | 三个工具各有一个明确的安全风险：外部 API 额度（web_search）、**SSRF**（http_request）、**任意代码执行**（code_exec），必须逐项落实防护 |

**验收一句话**：三个工具注册并出现在 `/api/status`；`web_search` 配置 Key 后可真实检索；`http_request` 能请求外网且**内网地址被 SSRF 防护拦截**；`code_exec` 在 Docker 环境中隔离执行代码（网络隔离 + 资源限制）；高风险工具（code_exec / http_post）正确触发 HITL 审批；全部既有测试回归通过。

---

## 1. 现状与目标

### 1.1 现有工具（`core/tools.py` 的 ToolRegistry 默认注册）

| 工具 | 风险 | 说明 |
|---|---|---|
| read_file / write_file / edit_file | low / medium | 本地文件操作 |
| exec（Shell） | high | 黑名单 + 超时 + 截断 |
| search_knowledge | low | RAG 本地检索 |

### 1.2 差距

- Agent 无法**访问外部世界**：不能搜网页、不能调外部 API、不能执行用户给的代码片段
- 三个新工具补齐这三块，同时引入三类新安全风险（见 0）

### 1.3 风险分级建议（新增）

| 新工具 | 风险 | 理由 |
|---|---|---|
| web_search | **low** | 只读外部信息，与 read_file 同级；用 max_results 限制控制成本 |
| http_request（GET） | **medium** | 只读外网；SSRF 防护在工具内部强制实施 |
| http_request（POST 等写方法） | **high** | 对外部资源有副作用，须人工审批 |
| code_exec | **high** | 执行任意代码，必须 HITL |

> 设计决策：`http_request` 拆成两个工具（`http_get` medium / `http_post` high），而不是一个工具两种风险——因为 HITL 是按**工具级** `risk_level` 触发的，工具内部无法动态决定是否审批。

---

## 2. 设计方案

### 2.1 工具一：`web_search(query, max_results=5)`

**职责**：调用搜索 API 返回清理后的网页摘要列表（标题/链接/摘要片段），供 Agent 基于实时信息回答。

**Provider 可插拔设计**（为后续换源留扩展点）：

```
web_search 工具
   └── SearchProvider 抽象接口: search(query, max_results) -> List[SearchResult]
         ├── TavilyProvider   (推荐默认，LLM 优化的搜索 API，免费额度 ~1000 次/月)
         ├── SerperProvider   (Google 搜索 API，备选)
         └── (预留) DuckDuckGoProvider（零 Key，稳定性差，仅本地兜底，可不实现）
```

**关键点**：
- Provider 通过 `config.yaml` 选择与配置 Key（见 2.4），Key 不落日志、不进 tool 参数
- 未配置 Key 时工具返回明确提示："web_search 未配置 API Key（tavily 或 serper）"
- 结果统一格式化为 Markdown 列表给模型（与 search_knowledge 输出风格一致），每条 ≤200 字，总量 ≤2KB
- 结果按工具执行结果回传，**不写文件**

### 2.2 工具二：`http_get(url, headers=None, timeout=15)`（medium）

**职责**：发起只读 HTTP GET，返回状态码 + 响应体（截断）。

**SSRF 防护（本任务的安全重点，必须实现）**：

```python
# 防护流程：解析 URL → 解析 DNS → 校验 IP → 禁止重定向绕过
def _is_ssrf_blocked(url: str) -> str | None:
    # 1. 仅允许 http/https
    # 2. host 解析出的所有 IP 不得是：
    #    回环(127.0.0.0/8, ::1)、私网(10/8, 172.16/12, 192.168/16)、
    #    链路本地(169.254/16, fe80::/10)、保留(0.0.0.0/8, 240.0.0.0/4, ::/128)
    # 3. 禁止重定向（或重定向后重新校验），防止 DNS rebinding / 302 跳内网
```

- 用 `httpx`（异步），`follow_redirects=False`，自行校验后**手动跟随一次重定向并重新校验**
- 响应体截断 20KB；`timeout` 默认 15s
- 工具执行失败返回可读错误（连接失败/超时/SSRF 拦截），不抛异常

### 2.3 工具三：`http_post(url, data=None, headers=None, timeout=15)`（high）

**职责**：提交 JSON/表单到外部 API（如调用第三方接口、发 webhook）。

- 复用 2.2 的 SSRF 防护（强制）
- `risk_level="high"` → 自动走 HITL 审批链路（执行前弹窗，超时自动拒绝）

### 2.4 工具四：`code_exec(code, timeout=30)`（high）

**职责**：在隔离沙箱中执行 Python 代码，返回 stdout/stderr。

**沙箱方案（按优先级）：**

| 方案 | 隔离强度 | 依赖 | 建议 |
|---|---|---|---|
| **Docker 容器**（推荐） | 强：网络隔离 + 资源限制 + 无持久化 | docker CLI 可用 | 默认方案 |
| Python 受限环境（resource + 禁用危险模块） | 弱（Python 不是安全沙箱） | 无 | 降级方案，**必须明确告知用户限制** |
| 同机 subprocess | 无 | 无 | ❌ 不安全，不采用 |

**Docker 沙箱运行方式**：

```bash
docker run --rm -i --network=none \
  --memory=256m --cpus=1 --pids-limit=128 \
  -v {workspace}/outputs:/sandbox \
  python:3.11-slim python -c "{code}"   # 或 stdin 传代码
```

- `--network=none`：**沙箱内无网络**（防数据外泄/挖矿类操作）
- `--memory/--cpus/--pids-limit`：资源限制，防资源耗尽
- `--rm`：执行完即销毁，无持久化
- 仅挂载 `outputs` 目录到 `/sandbox`（沙箱内代码只能读写该目录，与 2.5 的工作区规范一致）
- 超时：容器 30s 超时强制 `docker kill`
- 环境探测：启动时检测 `docker` 命令是否可用；不可用时返回"code_exec 需要 Docker 环境"，或降级到受限 Python 方案（工具说明里标注"非完全隔离，仅限可信代码"）
- **注意**：`code_exec` 是给"用户主动给的代码"用的（如"跑一下这段代码"），模型不应自主生成恶意代码——但安全边界仍按最坏假设设计

### 2.5 工作区规范（与现有系统提示一致）

新工具的所有产物（下载/生成文件）只允许写入 `workspace/outputs/`，由代码强制（如 http_get 不落盘、code_exec 挂载限制），不依赖模型自觉。

### 2.6 配置（`config.yaml.example` 新增 `tools` 段）

```yaml
tools:
  web_search:
    provider: tavily            # tavily | serper
    api_key: ""                 # 或环境变量 WEB_SEARCH_API_KEY
  code_exec:
    docker_image: "python:3.11-slim"
    enabled: true               # false 时工具返回"已禁用"
    fallback_unsafe: false      # Docker 不可用时是否降级到受限 Python（默认否）
```

环境变量优先级高于 config.yaml（与现有 LLM 配置风格一致）。

### 2.7 依赖与注册

- `requirements.txt` 显式加 `httpx>=0.24.0`（openai 虽已间接依赖，但显式声明是规范）
- 三个新工具文件建议放 `core/tools/` 包（当前是单文件 `core/tools.py`，可先不拆包，直接在 `core/tools.py` 内新增类；若文件过大再拆）
- `ToolRegistry.__init__` 注册新工具；`web_search`/`code_exec` 需要配置，通过注册时的可选参数传入（`ToolRegistry(config=...)` 或 setter）
- `/api/status` 的工具列表自动包含新工具（现有 `get_tools_summary` 无需改动）

---

## 3. 实施步骤

### Step 1：`requirements.txt`

- 加 `httpx>=0.24.0`

### Step 2：`core/tools.py` — SSRF 防护工具函数

- `_is_ssrf_blocked(url) -> str | None`（返回拦截原因或 None）：URL 解析 → socket 解析 host 全部 IP → 校验（含 IPv6）→ 防重定向绕过
- 单元测试先行（见 4.1）

### Step 3：`core/tools.py` — SearchProvider 与 web_search

- 抽象基类 `SearchProvider`（`search(query, max_results) -> list[dict]`）
- `TavilyProvider`（用 `httpx` 调 `https://api.tavily.com/search`，`api_key` 从配置读）
- `SerperProvider`（备选，`https://google.serper.dev/search`）
- `WebSearchTool(BaseTool)`：`risk_level="low"`，未配置 Key 时返回明确提示，结果格式化（≤2KB）

### Step 4：`core/tools.py` — http_get / http_post

- `HttpGetTool`（medium）：SSRF 防护 + 状态码/响应体（截断 20KB）+ 手动跟随一次重定向（重新校验）
- `HttpPostTool`（high）：同防护 + 支持 JSON/表单 body
- 统一错误处理（连接失败/超时/拦截原因都转成可读字符串返回）

### Step 5：`core/tools.py` — code_exec

- `CodeExecTool`（high）：Docker 沙箱执行（2.4 的命令），超时 `docker kill`；Docker 不可用按配置降级或返回提示
- stdout/stderr 合并返回，截断 20KB

### Step 6：`core/agent.py` / `app.py` — 配置注入

- 读 `config.yaml` 的 `tools` 段（环境变量优先），构造 `ToolRegistry` 时传入；`SessionManager.shared_components()` 里更新（`build_shared` 增加 tools 配置参数）
- 注意：`TinyAgent.build_shared` 当前签名是 `(workspace_dir, openai_api_key, base_url, model)`，需要扩展一个 `tool_config` 参数（保持向后兼容，默认 `{}`）

### Step 7：文档与回归

- README：特性表加 3 个工具、Roadmap 勾选；CHANGELOG [Unreleased]
- （可选加分）`eval/tasks/` 新增 1~2 个工具任务（如 `web_search_flow` mock 任务验证调用链）

---

## 4. 测试计划

### 4.1 单元测试（SSRF 防护——最高优先级）

| 用例 | 期望 |
|---|---|
| `http://127.0.0.1:8000/`、`http://localhost/`、`http://[::1]/` | 拦截（回环） |
| `http://10.0.0.1/`、`http://172.16.0.1/`、`http://192.168.1.1/`、`http://169.254.169.254/` | 拦截（私网/链路本地） |
| `http://evil.com` 解析到内网 IP（mock DNS） | 拦截（解析后校验） |
| `http://api.github.com` | 放行 |
| 非 http/https scheme（`file://`、`ftp://`） | 拦截 |

> mock DNS 方法：monkeypatch `socket.getaddrinfo` 或 `httpx` 的解析；测试不得真发网络请求（用 httpx `MockTransport` 或直接测防护函数）。

### 4.2 工具单元测试（mock 外部依赖）

- `web_search`：mock provider 返回固定结果 → 验证格式化输出、未配置 Key 的提示
- `http_get`：`httpx.MockTransport` 模拟 200/404/超时 → 验证返回格式与截断
- `code_exec`：无 Docker 环境时 `skip`；有则跑 `print("hello")` 验证 stdout 回传

### 4.3 集成测试

- ScriptedClient（`eval/fakes.py`）验证三个新工具的调用链（`tool_call_start/end` 事件、结果回传）
- 高风险工具（http_post / code_exec）触发 `approval_required` 事件（复用 test_hitl.py 的审批自动同意逻辑）

### 4.4 手动验证

- 配置 Tavily Key 后发"搜索一下今天的 AI 新闻"→ 工具返回摘要列表
- 请求 `http://127.0.0.1/` → 返回"被 SSRF 防护拦截"
- 发"执行这段代码：print(1+1)"→ HITL 审批 → Docker 沙箱执行返回 2

### 4.5 回归

- `python test_hitl.py`、`python test_compress.py`、`python eval/run_eval.py`（mock 5/5）
- 多会话 / 可观测性相关测试

---

## 5. 验收清单（Done 定义）

- [ ] 三个工具（或四个：http_get/http_post）注册并出现在 `/api/status` 工具列表
- [ ] `web_search`：配置 Key 后真实检索成功；未配置时返回明确提示
- [ ] `http_request`：外网 GET 成功；**回环/私网/链路本地地址全部被 SSRF 防护拦截**；重定向不绕过防护
- [ ] `code_exec`：Docker 沙箱执行成功，`--network=none` 网络隔离生效；超时被强制终止；Docker 不可用按配置处理
- [ ] 高风险工具（http_post / code_exec）走通 HITL 审批链路
- [ ] 新工具产物只落在 `workspace/outputs/`
- [ ] 单元 + 集成 + 回归测试全绿（无 Docker 环境的机器上 code_exec 测试 skip 而非失败）
- [ ] README（特性表/Roadmap）+ CHANGELOG 更新

---

## 6. 注意事项 / 坑

1. **SSRF 是最容易被追问的考点**：防的不只是字面上的 `127.0.0.1`，还有 DNS rebinding（域名解析到内网）、IPv6（`[::1]`）、重定向绕过（302 跳内网）、编码混淆（`127.0.0.1` 的八进制/十进制写法）。防护函数要**解析后校验 IP** 而不是字符串匹配 host
2. **code_exec 的降级方案要说清楚限制**：Python 受限环境不是安全沙箱，只在 `fallback_unsafe: true` 且明确告知用户时启用；默认 Docker 不可用就直接禁用
3. **搜索 API Key 不能进 tool 参数/日志**：Key 只在 provider 构造时读一次；工具参数里只有 query
4. **`http_get` 默认 `follow_redirects=False`**：手动跟随重定向并重新做 SSRF 校验，别信 httpx 自动跟随
5. **沙箱挂载路径**：只挂 `outputs`，沙箱内代码读写 `/sandbox` 即对应宿主机 outputs——与系统提示的工作区规范一致，别挂整个 workspace
6. **`ToolRegistry` 构造时机**：`build_shared` 是全局单例构建点，tools 配置要传进去；注意 `SessionManager.shared_components()` 只构建一次，配置改动需重启生效
7. **httpx 已是 openai 的间接依赖**，但显式声明进 requirements.txt 是规范，避免未来 openai 升级后断链

---

## 7. 前置阅读

- `core/tools.py`：BaseTool / ToolRegistry / SearchKnowledgeTool / ShellTool（风险分级与注册模式、_guard_command 黑名单写法）
- `core/agent.py`：`build_shared`（工具注册点、配置注入位置）
- `core/session.py`：`shared_components`（全局共享组件，tools 配置入口）
- `core/loop.py`：approval_required / tool_call_start / tool_call_end 事件（HITL 触发机制）
- `eval/fakes.py`：ScriptedClient / FakeStream（集成测试复用）
- `config.yaml.example`：配置段风格（llm / server）

---

## 8. 参考实现片段（仅示意，非最终代码）

```python
# SSRF 防护核心（tools.py 内工具函数）
import ipaddress, socket

BLOCKED_NETWORKS = [
    ipaddress.ip_network(n) for n in [
        "0.0.0.0/8", "10.0.0.0/8", "100.64.0.0/10", "127.0.0.0/8",
        "169.254.0.0/16", "172.16.0.0/12", "192.0.0.0/24", "192.168.0.0/16",
        "198.18.0.0/15", "224.0.0.0/4", "240.0.0.0/4", "::/128", "::1/128",
        "fc00::/7", "fe80::/10", "ff00::/8",
    ]
]

def _is_ssrf_blocked(url: str) -> str | None:
    from urllib.parse import urlparse
    parsed = urlparse(url)
    if parsed.scheme not in ("http", "https"):
        return f"仅支持 http/https，禁止 {parsed.scheme}"
    host = parsed.hostname or ""
    try:
        infos = socket.getaddrinfo(host, None)
    except Exception:
        return f"无法解析域名: {host}"
    for info in infos:
        ip = ipaddress.ip_address(info[4][0])
        if any(ip in net for net in BLOCKED_NETWORKS):
            return f"目标地址 {ip} 属于内网/保留地址，已拦截（SSRF 防护）"
    return None
```

```python
# WebSearchTool 结果格式化
def _format_results(results: list) -> str:
    parts = [f"搜索到 {len(results)} 条结果："]
    for i, r in enumerate(results[:5], 1):
        parts.append(f"{i}. **{r.get('title', '')[:100]}**\n   {r.get('url', '')}\n   {r.get('content', '')[:200]}")
    text = "\n\n".join(parts)
    return text[:2000]
```
