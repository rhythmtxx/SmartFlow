# SmartFlow 项目面试话术

> 配套学习项目：SmartFlow（https://github.com/rhythmtxx/SmartFlow）
> 用途：向面试官介绍项目时的叙事话术、30 秒模板与追问准备。
> 与《面试复习手册》的分工：《面试复习手册》讲**代码知识点**（每个文件怎么写的），本文件讲**怎么开口说**（如何组织语言、如何应对追问）。

---

## 目录

- [一、项目一句话定位](#一项目一句话定位)
- [二、为什么设计评估集（核心专题）](#二为什么设计评估集核心专题)
- [三、亮点功能面试话术](#三亮点功能面试话术)
  - [3.1 HITL 人工审批机制](#31-hitl-人工审批机制)
  - [3.2 多会话隔离](#32-多会话隔离)
  - [3.3 可观测性面板](#33-可观测性面板)
  - [3.4 RAG 知识库](#34-rag-知识库)
  - [3.5 流式输出与多后端兼容](#35-流式输出与多后端兼容)
  - [3.6 上下文压缩](#36-上下文压缩)
  - [3.7 更多工具（Web 搜索 / HTTP / 代码沙箱）](#37-更多工具web-搜索--http--代码沙箱)
  - [3.8 前端工程化（React + Vite 迁移）](#38-前端工程化react--vite-迁移)
- [四、简历写法参考](#四简历写法参考)
- [五、面试红线提醒](#五面试红线提醒)

---

## 一、项目一句话定位

**标准版（约 120 字）：**

> 独立开发的轻量级 AI Agent 框架，基于 ReAct 模式实现大模型多轮推理与工具调用，内置 Human-in-the-Loop 人工审批、RAG 知识库、多会话隔离与流式输出，并构建 Eval 评估集量化 Agent 行为质量。技术栈：Python / FastAPI / OpenAI 兼容 API / SQLite / ChromaDB / SSE + React / TypeScript / Vite。

**精简版（约 40 字，放标题下方）：**

> 基于 ReAct 的多工具 AI Agent 框架，支持 HITL 人工审批、RAG 知识库与多会话隔离，配套 Eval 评估集与可观测性面板。

**成果导向版（突出量化）：**

> 独立开发的多工具 AI Agent 框架（FastAPI + OpenAI 兼容 API）：实现 HITL 人工审批、RAG 知识库、多会话隔离等生产级能力，并以自建 Eval 评估集回归质量（5/5 端到端任务通过，平均 2.4 轮工具调用），配套成本估算与工具调用时间线可视化。

**使用原则**：简介负责"总"（定位 + 技术栈），分条负责"分"（怎么实现的证据链），两者不要重复。

---

## 二、为什么设计评估集（核心专题）

> 面试官追问"为什么做评估集"的完整答题逻辑，分三层。

### 2.1 第一层：LLM 应用是非确定性的，传统测试逻辑失效

写普通软件：改一行代码 → 跑单元测试 → 绿了放心，因为**同一个输入永远有同一个输出**。

Agent 不一样：同一个 prompt，模型每次返回的文本、是否调工具、调哪个工具、参数是什么，**都可能不一样**。于是：

- 改了一版 system prompt，怎么知道变好还是变坏？——靠"我觉得"？
- 加了一个新工具，怎么知道它没让 Agent 在其他任务上开始乱调？——靠肉眼试几个 case？
- 这周调好的，下周改 bug 又改回去了，怎么发现？——靠记忆？

**"感觉变好了"不可复现、不可对比、不可回归。** Agent 工程化的第一步，就是把"感觉"变成"数字"。

### 2.2 第二层：评估集解决了四个问题

| 问题 | 评估集的解法 |
|---|---|
| 不可复现 | 同一组任务、同样输入，每次跑都有记录，结果可重复 |
| 不可对比 | 改 prompt / 换模型 / 加工具前后各跑一轮，成功率、平均轮数、token 直接对比 |
| 不可回归 | 接 CI：每次提交自动跑 mock 集，链路被改坏立刻红灯 |
| 不可量化 | 汇报时是"5/5 通过、平均 2.4 轮"，不是"我觉得还行" |

### 2.3 第三层：为什么设计成 mock + real 双模式

| 模式 | 机制 | 回答的问题 | 成本 |
|---|---|---|---|
| **mock** | 脚本化 FakeClient 让模型"按剧本调工具"，验证工具调用链是否正确执行、结果是否正确回传（含 HITL 审批链路） | 链路是否正确（有没有坏） | 零，可接 CI |
| **real** | 真实模型跑完整链路，按任务声明的 checks 校验最终产物（输出文件存在/关键词、回复关键词），统计 token | 效果是否达标（有没有效） | 需要 API Key |

两层互补：**mock 管"没坏"，real 管"有效"**。mock 集在没有 API Key 的环境也能跑，这是设计时的实际考量。

### 2.4 30 秒面试模板（可背）

> "Agent 和传统软件最大的区别是**非确定性**——模型输出每次都不一样，所以'改完感觉变好了'是没法验证的。我给 SmartFlow 建了 7 个端到端评估任务，分两层：**mock 模式**用脚本化客户端验证工具调用链，零成本、能接 CI 做回归；**real 模式**用真实模型校验最终产物，统计成功率、平均轮数和 token。这样我每次改 prompt 或加工具，跑一轮 `python eval/run_eval.py` 就有数据说话——比如现在的基线是 5/5 通过、平均 2.4 轮工具调用。这不是'我觉得'，是跑出来的。"

### 2.5 追问准备

**Q：mock 模式的模型调用是假的，验证还有什么意义？**
> mock 验证的不是模型"想不想"用对工具，而是**工具链路本身**——模型说了调 read_file，代码是不是真的去读文件、结果有没有正确回传给模型、tool_call 的 id 链是否完整。链路一旦断裂（比如改坏了 Buffer 拼接），真实模型跑 100 次也只会得到一个共性错误。mock 集就是干这个的：确定性、零成本、第一时间报警。

**Q：评估任务怎么保证覆盖足够？**
> 按工具类型和组合覆盖：单一工具（read/write/exec）、链式调用（read→edit、read→write）、高风险审批路径（exec）、RAG 检索。真实模式覆盖产物校验。后续可以按业务场景继续加任务，JSON 声明式，加一个任务就是一个文件。

**Q：real 模式的结果不稳定怎么办？**
> 非确定性本身是 Agent 的属性，评估集要做的是**把不稳定变成可统计**——多跑几轮看成功率分布，而不是要求 100% 单次通过。这也是为什么报告输出成功率而不是"通过/不通过"二元结论。

**Q：成功率多少算合格？**
> 没有绝对标准，看任务难度和模式。mock 链路测试要求 100%（确定性链路没有理由失败）；real 效果测试看基线对比——重点是我有**基线**可以对比，改完是升是降一目了然。

### 2.6 一句话总结

> 评估集不是"测试"，是给**不可预测的 Agent 装上可测量的标尺**——让"好不好"从主观感受变成客观数字，这是 Agent 工程化和普通 demo 的分水岭。

---

## 三、亮点功能面试话术

### 3.1 HITL 人工审批机制

**客观定位（先声明，避免被认为不懂业界）：**
> Human-in-the-Loop 是 Agent 安全控制领域的常见设计模式，OpenAI Function Calling、LangChain 等都有类似机制。我的项目做的是**轻量异步实现**。

**30 秒话术：**
> "核心思路：AgentLoop 要执行高风险工具前，先发一个 approval_required 事件通过 SSE 推给前端弹窗，然后用 `asyncio.Event` 挂起当前协程等待用户响应。用户点同意/拒绝后，前端 POST /api/approve，后端 `event.set()` 唤醒协程，根据结果执行或跳过工具。三个关键设计：一是**异步非阻塞**——挂起的是单个工具调用协程，不阻塞服务器，同一进程能并行服务多个请求；二是**超时自动拒绝**（60 秒），安全兜底；三是**审批状态按会话隔离**，多用户互不干扰。我写了 Mock 测试覆盖同意、拒绝两条完整链路。"

**追问准备：**
- Q：为什么用 asyncio.Event 而不是直接 await input？→ 因为要跨协程通信：loop 协程和 /api/approve 是独立协程，无法直接共享变量，必须通过共享的 pending 字典 + Event 间接通信（可引用 tools.py 的 ApprovalManager 设计）。
- Q：超时怎么处理？→ `asyncio.wait_for(event.wait(), timeout=60)`，超时抛 TimeoutError 返回 False（默认拒绝），finally 里清理 pending 防止内存泄漏。

#### 3.1.1 三层风险纵深防御（追问：风险是怎么评估并设计的）

**先对齐：项目里的安全控制不是三个并列的黑名单，而是按"风险发生阶段"划分的纵深防御**

| 层 | 阶段 | 机制 | 拦截对象 |
|---|---|---|---|
| 第一层 | 执行前·代码拦截 | Shell 黑名单（正则 deny_patterns） | **确定性危险**：rm -rf、format、dd、shutdown、fork bomb… |
| 第二层 | 执行前·人工审批 | HITL 按工具 risk_level 分级 | **需要人判断的风险**：黑名单外的未知危险命令 |
| 第三层 | 执行中/后·运行时兜底 | 执行超时 + 输出截断 + max_iterations=10 | **失控行为**：命令卡死、输出爆炸、无限循环 |

**设计时的评估逻辑（三个问题）：**

1. **这个风险"可枚举"吗？——决定用代码拦截还是用人**
   - `rm -rf`、`format` 这类危险是**可穷举的模式** → 正则黑名单：确定性、零成本、不打断流程
   - 但黑名单只能防已知，模型可能想出任何没覆盖的命令 → **不可枚举的部分交给"人"判断**

2. **打断用户的成本 vs 安全收益怎么权衡？——决定风险分级**
   - 全部工具都弹窗 → 用户体验毁灭（每句话都点确认）；全不放行 → 安全形同虚设
   - 按**可逆性**分级：`low`（read_file 只读）直接放行；`medium`（write/edit 可恢复）直接放行；`high`（exec 不可逆）才弹窗
   - 核心：**把有限的人工注意力花在边际收益最高的地方**

3. **人不在场时怎么办？——决定兜底策略**
   - 审批加 60s 超时**自动拒绝**：用户失联时按最安全假设处理（fail-safe）
   - 运行时全自动兜底：命令卡死被超时杀掉、输出爆炸被截断、循环被迭代上限打断——这些层不需要人参与

**三层怎么配合（纵深防御的本质）：**

```
模型想执行 exec("rm -rf /")
   ↓
① 黑名单正则匹配 → 直接拦截，返回错误给模型（根本到不了审批层）
   ↓（假设是黑名单没覆盖的命令）
② HITL 弹窗 → 用户看到命令内容 → 同意才执行 / 拒绝跳过 / 60s 超时自动拒绝
   ↓（用户点了同意）
③ 执行中 → 超时 60s 杀进程 + 输出 1 万字符截断 → 结果回传模型
```

每层都有独立兜底，单层失效不等于裸奔：黑名单漏网 → 审批兜住；审批没人理 → 超时拒绝兜住；执行失控 → 运行时兜住。

**30 秒面试模板（可背）：**
> "我按**风险发生的阶段**设计了纵深防御。执行前有两道闸：第一道是代码层黑名单，对 rm -rf、format 这类**确定性危险**做正则拦截，零成本不打断流程，但只能防已知模式；第二道是 HITL 人工审批，兜住**需要人判断的未知风险**——按工具的可逆性分级，只读和可恢复的直接放行，只有不可逆的 exec 才弹窗，60 秒超时自动拒绝。分级而不是一刀切，是因为**全部审批会烦死用户，全不放行等于没有安全，要把人工成本花在边际收益最高的地方**。执行中还有第三道纯代码兜底：超时、截断、迭代上限。三层各管一段：黑名单拦住确定的，审批拦住未知的，超时兜住失控的。"

**追问准备：**
- Q：medium 风险为什么不审批？→ 因为可恢复——write 写错了可以再写回去。审批的本质成本是打断用户，收益是防止不可逆损失，medium 的收益覆盖不了成本。
- Q：黑名单会不会误伤合法操作？→ 会，这是设计取舍。黑名单是"拦截 + 提示"而非"静默丢弃"：被拦截的命令会作为工具错误结果回传给模型，模型能告知用户"被安全策略拦了"。安全默认宁可误伤，不可放过。
- Q：审批超时为什么定 60 秒？→ 太短用户来不及看完命令内容；太长挂起的协程占用资源。60 秒是"看完一条命令 + 犹豫"的合理时长，且超时默认拒绝是安全方向。
- Q：模型能不能绕过审批？→ 不能。模型对工具只有"请求权"没有"执行权"——它输出 tool_call，真正执行的是代码层的 ToolRegistry，审批是执行路径上的强制关卡，不是模型可跳过的建议。

### 3.2 多会话隔离

**30 秒话术：**
> "第一版是全局单例，所有用户共享一份记忆和 token 统计，互相污染。重构后我用 SessionManager 做了分层：**无状态组件（LLM client、技能、工具、知识库）全局共享只构建一次，只有记忆和审批状态按 session_id 隔离**——因为 SQLite 表结构当初就带 session_id 列，所以数据库零改动，纯组装层改造。同时处理了三个并发边界：审批按会话路由、同会话串行锁、锁覆盖整个流式生成周期防止插入新请求破坏工具链。"

**追问准备：**
- Q：为什么无状态组件能全局共享？→ LLM client 只是发请求的连接对象；技能/工具/知识库是只读配置；共享的只有会话相关的记忆和审批状态。共享避免每个会话重复加载 embedding 模型和扫描技能目录。
- Q：同会话并发会怎样？→ 两个请求同时写 MemoryStore 会乱序，甚至流式过程中插入新请求会破坏工具调用链，所以每会话一把 asyncio.Lock，锁范围覆盖整个流式生成周期。

### 3.3 可观测性面板

**30 秒话术：**
> "Agent 生产运行是黑盒，必须有可观测性。我在 SQLite 里加了 tool_calls 和 rounds 两张表，在 chat_stream 事件循环里自动采集——工具调用（名称/参数/结果）和每轮 token 明细，不用埋点代码。然后做了 `/api/stats` 接口：token 总量、轮数、工具调用次数、**成本估算**（价格可配置）、工具调用时间线。前端侧栏实时展示成本数字和按风险着色的工具调用列表。这对应可观测性的 Logs / Metrics / Traces 三要素的轻量实现。"

### 3.4 RAG 知识库

**30 秒话术：**
> "用 sentence-transformers 做本地向量化，ChromaDB 做存储检索。文档导入时先切片——段落优先、固定长度兜底、带 50 字重叠防止语义截断——再批量向量化存入。检索时把用户问题向量化，余弦相似度召回 Top-K 片段（含来源和相似度分数）作为工具结果返回给 Agent。全程本地运行不消耗额外 API。我写了 6 个测试覆盖空库、导入、检索、不相关问题等场景。"

**追问准备：**
- Q：为什么切片要有重叠？→ 防止一句话被切断在两个 chunk 边界，重叠 50 字保证边界语义连续。
- Q：为什么用余弦相似度不用欧氏距离？→ 欧氏距离受向量长度影响；余弦只看方向，与文本长短无关，语义检索更准。
- Q：RAG 有什么局限？→ 检索质量依赖切片策略；Embedding 对专业领域词汇效果差；可能需要 Reranker 二次排序。

### 3.5 流式输出与多后端兼容

**30 秒话术：**
> "基于 AsyncGenerator + SSE 做全链路流式，模型回复、推理过程、工具执行状态都能实时推给前端。流式 tool_call 的参数是碎片化的，我设计了 Buffer 拼接逻辑按 index 重组。这里踩过一个真实的坑：部分后端会在多个分片重复发送完整函数名，直接拼接会变成 'read_fileread_file'，修复方案是只在名称为空时赋值——这个 bug 是真实跑多后端才遇到的。"

### 3.6 上下文压缩

**30 秒话术：**
> "长对话超过 20 条窗口后，早期的关键信息（用户偏好、关键事实）会被粗暴截断丢掉。我的方案是用 LLM 摘要压缩：当活跃消息超过阈值（24 条）时，把最早一批**完整轮次**（最多 10 条）交给 LLM 压成 ≤300 字摘要，存到 SQLite 的 summaries 表，然后**先写摘要、成功后再删原文**；摘要注入 system prompt 的『早前对话摘要』段，模型依然能感知早期上下文。三个关键设计：一是**轮次完整性**——只压缩从 user 开始、到该轮结束的闭环，绝不从 tool 消息中间切开，否则 API 会报孤儿 tool 错误；二是**fail-safe**——摘要调用失败就跳过，一条消息都不删、不阻塞对话；三是**按会话隔离**，多用户摘要互不干扰。"

**追问准备：**
- Q：为什么不直接把窗口调大？→ 上下文窗口是有限的，全量塞入会迅速挤占预算、还超长报错。压缩是"用 token 换记忆"：摘要占 300 字，远小于原始 10 条消息的成本，还能保留早期关键信息。
- Q：为什么"先存摘要再删原文"？→ 保证原子性：如果先删原文再生成摘要，摘要失败就永久丢数据了。反过来，摘要落库成功才删原文，任何一步失败都安全回退。
- Q：怎么保证不切到 tool 链中间？→ `get_compress_candidates` 按轮次边界扫描：候选必须从 user 开始；如果一批的末尾是 tool 消息，就回退到该轮 assistant 为止，绝不让 tool 结果单独成为孤儿。这个规则有 5 个单元测试覆盖。
- Q：摘要本身会越积越多吗？→ 每条 ≤300 字，多轮对话下来也就几条；按时间正序全部注入，总量可控。真到极端长对话，还可以对旧摘要做二级摘要（摘要的摘要），但当前规模不需要。
- Q：压缩会消耗额外 token 吗？→ 会，但这是有意的取舍：摘要调用一次的成本远低于每次对话都携带全部历史。也可以把摘要调用的 token 计入可观测性的 rounds 表。

### 3.7 更多工具（Web 搜索 / HTTP / 代码沙箱）

**30 秒话术：**
> "我给 Agent 加了三个能力边界之外的工具，每个都对应一个明确的安全风险，安全设计是这部分的重点。**web_search** 接 Tavily 搜索 API，Provider 可插拔（Tavily/Serper），结果统一格式化给模型。**HTTP 请求拆成了两个工具**——`http_get`（medium）只读、`http_post`（high）有副作用走 HITL 审批，因为 HITL 是按工具级风险触发的，一个工具没法内部动态决定要不要审批。**SSRF 防护**是核心：`_check_ssrf` 不是字符串匹配 host，而是解析 URL、DNS 解析出**所有 IP**、逐个校验是否属于回环/私网/链路本地/保留地址（含 IPv6），重定向也手动跟随并重新校验，防 DNS rebinding 和 302 跳内网。**code_exec** 用 Docker 沙箱执行代码：`--network=none` 断网、`--memory=256m --cpus=1 --pids-limit=128` 资源限制、`--rm` 执行完销毁、只挂载 outputs 目录——防数据外泄和资源耗尽，无 Docker 环境时安全降级为禁用而不是裸跑。"

**追问准备：**
- Q：SSRF 防护具体怎么做的？→ 三层：① URL 解析，只放行 http/https；② `socket.getaddrinfo` 解析出 host 的全部 IP，逐个用 `ipaddress` 判断是否在拦截网段（127/8、10/8、172.16/12、192.168/16、169.254/16、fc00::/7、fe80::/10 等，含 IPv6）；③ 禁止自动跟随重定向，手动跟随一次并**重新校验**新 URL，防止 302 跳内网。关键是不信 host 字符串，信解析后的 IP。
- Q：为什么 http 要拆成两个工具而不是一个？→ HITL 审批在 `AgentLoop` 层按工具的 `risk_level` 触发，工具内部拿不到审批能力。GET 只读和 POST 有副作用风险差一个量级，拆开才能让 GET 免审批、POST 强制审批。
- Q：code_exec 为什么用 Docker 而不是 subprocess 或受限 Python？→ subprocess 同机执行没有隔离，恶意代码能读整个文件系统；受限 Python（resource 模块 + 禁 import）也不是安全沙箱，Python 语言层面防不住。Docker 是成本可接受的真隔离：网络隔离 + 资源限制 + 无持久化。无 Docker 时我选择**禁用并提示**，而不是降级到不安全方案裸跑。
- Q：沙箱里能访问外网吗？→ 不能，`--network=none` 完全断网。这样即使代码被诱导执行恶意脚本，也无法外传数据或发起攻击。
- Q：web_search 为什么选 Tavily？→ 它是专门为 LLM 设计的搜索 API，返回清理后的网页摘要（不是原始 HTML），省去解析；有免费额度。Provider 抽象成接口，换 Serper 或自建只改一个类。
- Q：这些工具怎么保证不滥用？→ 三层：风险分级 + HITL（http_post/code_exec 强制人工审批）、参数上限（max_results、timeout、响应截断 20KB）、产物路径强制落在 outputs 目录。测试覆盖了 15 个用例，包括 SSRF 拦截、重定向绕过、HITL 链路、无 Docker 降级。

### 3.8 前端工程化（React + Vite 迁移）

**30 秒话术：**
> "前端最初是 1100 行单文件 HTML（CDN 依赖 + 全局函数），我把它渐进迁移成了 React + TypeScript + Vite 工程化项目。核心工作：一是**组件化**——把 12 个交互区拆成组件树（聊天区/审批弹窗/会话面板/遥测面板等），配套 6 个自定义 hooks；二是**类型契约**——SSE 事件用 TS 判别联合类型定义（`text_delta | tool_call_start | approval_required | ...`），switch 分支漏掉一种事件编译直接报错；三是**流式 Hook**——`useChatStream` 用 fetch + ReadableStream 增量解析 SSE，特意不用 EventSource，因为它带不了 Authorization 头、只能 GET；四是**状态管理**——Zustand 管理会话/Token/消息，localStorage key 与旧版兼容。迁移是**分 5 个阶段渐进式**的，每阶段可运行可回退，不是大爆炸重写。Vitest 17 个用例覆盖了流式解析边界（半个 data 行跨 chunk）、审批倒计时、鉴权头。生产由 FastAPI 伺服构建产物，开发用 Vite proxy 联调。"

**追问准备：**
- Q：为什么 SSE 用 fetch 而不是 EventSource？→ EventSource 只能 GET、无法自定义请求头，而我们的 /api/chat 是 POST 且需要带 Authorization（鉴权开启时）。fetch + ReadableStream 手动解析 `data:` 行，两者都满足。
- Q：判别联合类型具体好在哪？→ 定义 `ChatEvent` 为带 `type` 字段的联合，`switch(event.type)` 时 TS 会穷尽检查——未来后端新增事件类型，前端漏处理会编译报错而不是运行时静默丢事件。
- Q：为什么选 Zustand 不选 Redux？→ 项目规模用 Redux 是杀鸡用牛刀（模板代码多）。Zustand 约 1KB、无 Provider 嵌套、hooks 式调用，且测试好写。
- Q：渐进迁移怎么保证不破坏线上？→ 5 个阶段（骨架→静态面板→交互面板→聊天+审批→收尾），每阶段构建 + 手动验证通过才进下一阶段；旧 `static/` 直到验收前保留，随时可回退。
- Q：切换会话的竞态怎么处理？→ 场景：快速切换会话时，上一个会话的 stats/history 请求晚返回会覆盖新会话的界面。用会话标识守卫：请求发起时记录 session，返回时若 session 已变则丢弃；聊天流用 AbortController 主动取消。
- Q：测试覆盖了什么？→ 7 个测试文件 17 个用例：SSE 增量解析（含半个 `data:` 行跨 chunk、非法 JSON 不崩溃）、审批弹窗倒计时与提交、消息渲染、client 鉴权头、会话 store 持久化。核心交互逻辑都有单测。

---

## 四、简历写法参考

### 4.1 功能亮点分条（9 条版；第 9 条对 FDE 岗重点展示，AI 应用开发岗可视篇幅取舍）

1. 基于 ReAct 模式实现 Agent 循环引擎，支持多轮推理与自主工具调用；以 `max_iterations=10` 循环上限、Shell 命令黑名单 + 执行超时 + 输出截断构建多层安全控制。
2. 设计并实现 Human-in-the-Loop 人工审批机制：以 `asyncio.Event` 挂起高风险工具协程、SSE 实时推送审批请求，支持同意/拒绝/60s 超时自动拒绝，审批按会话隔离；Mock 测试覆盖完整审批链路。
3. 集成 RAG 知识库：sentence-transformers 本地向量化 + ChromaDB 存储检索，实现带重叠的段落级切片与余弦相似度召回，Agent 可基于内部文档作答（相关检索相似度 0.76+），减少幻觉。
4. 基于 AsyncGenerator + SSE 实现全链路流式输出；针对流式 Tool Call 参数碎片设计 Buffer 拼接逻辑，并修复多后端重复发送函数名导致的拼接错误，兼容 OpenAI/DeepSeek/通义千问，首字响应 < 1s。
5. 设计 SessionManager 多会话架构：无状态组件全局共享仅构建一次，记忆与审批按 session_id 隔离，配合每会话 asyncio.Lock 保证并发串行，多用户互不污染。
6. 实现基于 LLM 摘要的上下文压缩：超过阈值（24 条）时将最早完整轮次压缩为 ≤300 字摘要注入系统提示，按轮次边界截止避免切断工具链，先写摘要再删原文保证失败安全，长对话早期上下文不再丢失。
7. 扩展 Web 搜索（Tavily Provider 可插拔）、HTTP 请求与代码执行沙箱三类工具：实现基于 IP 网段校验的 SSRF 防护（含 IPv6 与重定向绕过拦截），高风险工具（http_post/code_exec）强制 HITL 审批；code_exec 基于 Docker 沙箱（网络隔离 + 内存/CPU/进程数限制 + 无持久化）。
8. 构建 Eval Harness 评估集：JSON 声明式任务 + Mock/真实 LLM 双模式，自动校验工具调用链与输出产物，输出成功率/平均轮数/token 报告（5/5 通过、平均 2.4 轮），可接入 CI；配套 /api/stats 可观测性接口（token 明细 + 成本估算 + 工具调用时间线）。
9. 将单文件前端（1100 行 HTML + CDN 依赖）渐进重构为 React + TypeScript + Vite 工程：按 12 个交互区拆分为组件树与 6 个自定义 hooks，用 TS 判别联合类型定义 SSE 事件契约，Zustand 管理会话/鉴权/消息状态；封装 useChatStream 流式解析 Hook（fetch + ReadableStream，规避 EventSource 无法携带鉴权头的限制），实现会话切换竞态防护；Vitest 17 个用例覆盖流式解析/审批弹窗/鉴权链路，ESLint/Prettier 规范代码质量，生产由 FastAPI 伺服构建产物。

### 4.2 未实现功能的写法（动词红线）

| 动词 | 含义 | 能否用于未实现功能 |
|---|---|---|
| 实现 / 开发 / 落地 | 我做完了，能跑 | ❌ 绝对不能 |
| 设计 / 规划 / 提出方案 | 我想清楚了怎么做 | ✅ 可以 |
| 调研 / 对比 / 选型 | 我研究过业界方案 | ✅ 可以 |
| 发现 / 识别（问题与局限） | 我看到现有方案的问题 | ✅ 可以（很加分） |

三种安全写法（以"上下文压缩"为例，若未实现）：

- **方式一：下一步规划（最安全）**
  > 项目后续规划：基于 LLM 摘要的历史上下文压缩、Web 搜索与 HTTP 工具扩展、评估集接入 CI。
- **方式二：发现问题 + 给出方案（最像资深工程师）**
  > 发现现有 20 条历史窗口的粗暴截断会丢失关键信息，设计并规划了基于 LLM 摘要的上下文压缩方案：对完整轮次（user→assistant→tool 成对）做 ≤300 字摘要并注入系统提示，避免从 tool_call 中间截断导致 API 报错。
- **方式三：调研结论**
  > 调研对比 LangChain / LlamaIndex 的上下文管理与记忆方案，明确自研轻量框架的取舍（零依赖、可控性强）。

---

## 五、面试红线提醒

1. **时间线对齐**：简历写 2025.12–2026.04，但仓库 commit 集中在 2026.05–08。面试官点开仓库看提交记录会对不上，投递前务必对齐时间。
2. **数字要能扛住追问**：
   - "5/5 通过、平均 2.4 轮"是 mock 评估集真实跑出来的 ✅
   - "相似度 0.76+"来自 RAG 测试样本（"RAG 有什么优势"检索场景）✅
   - "首字响应 < 1s"是设计目标/实测均值，被追问时说明场景
3. **"原创"是雷区**：HITL、RAG 都是业界成熟模式，永远不要宣称"原创"，重点讲**实现思路与取舍**（详见 3.1）。
4. **"生产级"慎用**：只有在能说出证据（多会话、鉴权、评估集、可观测性）时才用；否则用"轻量级框架"更稳。
