# 上下文压缩（Context Compression）任务大纲

> **任务交接文档**：本大纲供接手此任务的开发者使用，包含现状分析、设计方案、实施步骤、测试计划与验收标准。实现前请先阅读文末「前置阅读」列出的代码文件。

---

## 0. 任务说明

| 项 | 内容 |
|---|---|
| 任务 | 用 LLM 摘要压缩替代当前"超窗口直接丢弃"的历史截断 |
| 目标 | 长对话中，早期关键信息（用户偏好、关键事实、已完成操作）不因窗口截断而丢失 |
| 关联 Roadmap | README「Roadmap」→ 上下文压缩 |
| 预计工作量 | 0.5~1 天（数据层 + 摘要注入 + 触发逻辑 + 测试） |

**验收一句话**：超过窗口阈值的旧消息被压缩成 ≤300 字摘要并注入系统提示，模型仍能感知早期上下文；所有边界条件（轮次完整、失败兜底、并发）安全；既有测试全部回归通过。

---

## 1. 现状与问题

### 1.1 当前行为（要替换的部分）

`core/memory.py` 的 `get_messages(window_size=20)`：

```python
def get_messages(self, window_size: int = 20):
    if len(self.messages) <= window_size:
        return self.messages
    start_idx = max(0, len(self.messages) - window_size)
    while start_idx > 0 and self.messages[start_idx].get("role") != "user":
        start_idx -= 1
    return self.messages[start_idx:]
```

- 超过 20 条时，**窗口外的历史直接丢弃**
- "从最近的 user 消息开始截断"只是防孤儿 tool_call 的修补（保证发给 API 的消息链合法），不是信息保留方案

### 1.2 问题

- 长对话早期说过的关键约束/事实，20 条之后模型就"失忆"了
- 丢弃不可逆，用户无法回溯

### 1.3 目标行为

```
[旧消息] →(超过阈值)→ LLM 摘要(≤300字) → 存入 summaries 表 → 删除被压缩的原始消息
                                                              ↓
              ContextBuilder.build_system_prompt 注入: ## 早前对话摘要 {summary}
```

---

## 2. 设计方案

### 2.1 数据模型（SQLite：workspace/memory/memory.db）

**不修改 messages 表结构**（避免对既有数据库做 ALTER 的兼容处理），采用"先存摘要、成功后再删原文"：

```sql
-- 新增表
CREATE TABLE IF NOT EXISTS summaries (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT    NOT NULL,
    content    TEXT    NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_summaries_session ON summaries(session_id, id);
```

> 备选方案（不推荐）：messages 表加 `compressed` 标记列。需要对已存在的库做 `PRAGMA table_info` 检查 + ALTER，且保留原文会继续占用存储与查询，收益低。

### 2.2 压缩触发

| 项 | 建议值 | 说明 |
|---|---|---|
| 触发时机 | 每次对话 `turn_end` 事件后（`core/agent.py` 的 `chat_stream` 内） | 当前请求的流已发完，压缩不会影响用户体验 |
| 阈值 | 活跃消息数 > 24 | 建议常量 `COMPRESS_THRESHOLD = 24`（可后续移到 config.yaml） |
| 单批压缩量 | 最早 10 条 | 一次只压缩一批，压缩后回落，避免每次对话都触发 |
| 防重入 | `self._compressing` 标志或复用会话锁 | 防止并发请求同时触发压缩 |

### 2.3 摘要生成

- **复用主 LLM client**（`TinyAgent.client`，纯文本 completion，不涉及工具与 HITL）
- Prompt 模板（中文）：

```
请将以下对话历史压缩成不超过 300 字的摘要。必须保留：
1. 用户的偏好与关键要求
2. 已完成的重要操作与结果
3. 未完成或待办的事项
忽略寒暄与无关细节。直接输出摘要，不要其他内容。

--- 对话历史 ---
{最早一批完整轮次的原始消息，按序拼接，长 tool 结果截断到 200 字}
```

- **失败兜底（fail-safe）**：LLM 调用异常 → 跳过本次压缩，**不删除任何消息**，不阻塞对话

### 2.4 摘要注入

`core/context.py` 的 `build_system_prompt()`，在"长期记忆"段之后追加：

```python
# 4. 挂载早前对话摘要（上下文压缩产物）
summaries = self.memory.get_summaries()
if summaries:
    parts.append("# 早前对话摘要\n\n" + "\n\n".join(f"- {s}" for s in summaries))
```

- 多条摘要按时间正序全部保留（摘要本身很小，不占多少 token）
- 摘要与 `get_messages` 窗口截断**并行**：窗口内活跃消息照常给，窗口外靠摘要补

### 2.5 边界条件（必须全部处理）

1. **轮次完整性（最关键）**：只能压缩"完整轮次"——从 `user` 消息开始，到该轮结束（assistant 回复；若末尾是 `tool` 消息则必须连同其对应的 assistant tool_call 一起，不能把 tool 结果单独留在后面）
   - 实现建议：候选消息按序扫描，遇到 `user` 开始收集；**一批的末尾不能是 `tool` 消息**；若末条是 tool，向前回退到该轮 assistant 为止
2. **不重复压缩**：被删除的消息天然不在候选；摘要一旦生成成功才删除原文
3. **摘要长度**：≤300 字，模型输出超长则截断
4. **失败兜底**：见 2.3，任何异常都不删消息
5. **多会话隔离**：summaries 表按 `session_id`，MemoryStore 已按会话隔离，天然支持
6. **`/api/history` 渲染**：被压缩的消息不再出现在历史里（可接受；前端不额外提示，如需可加"已压缩 N 条历史"提示）

---

## 3. 实施步骤

### Step 1：`core/memory.py` — 数据层

- `_init_db()`：新增 `summaries` 建表 SQL
- 新方法：
  - `get_summaries() -> List[str]`：按 id 正序返回当前会话全部摘要内容
  - `append_summary(content: str) -> None`：写入一条摘要
  - `get_compress_candidates(max_messages: int = 10) -> List[int]`：返回最早可压缩的**完整轮次**消息 id 列表（实现 2.5-1 的轮次规则；返回空列表表示无候选）
  - `delete_messages_by_ids(ids: List[int]) -> None`：删除指定 id 的消息（同时从内存缓存 `self.messages` 移除）
- `clear_history()`：追加 `DELETE FROM summaries WHERE session_id = ?`

### Step 2：`core/context.py` — 摘要注入

- `build_system_prompt()`：按 2.4 追加摘要段（注意：当前方法只接收 memory/skills，`self.memory.get_summaries()` 直接可用）

### Step 3：`core/agent.py` — 压缩触发

- `chat_stream` 的 `turn_end` 分支末尾调用 `self._maybe_compress()`
- 新增方法 `_maybe_compress()`：
  1. `if self._compressing: return`（防重入标志，`__init__` 中初始化 `self._compressing = False`）
  2. `self._compressing = True`，`try/finally` 复位
  3. 消息数 ≤ 阈值 → 直接 return
  4. `candidates = self.memory.get_compress_candidates()`，空则 return
  5. 组装历史文本 → 调 `self.client.chat.completions.create(...)`（非流式）→ 取摘要文本
  6. `self.memory.append_summary(summary)` 成功后再 `self.memory.delete_messages_by_ids(candidates)`
  7. 任何异常：`logging.warning` 并静默跳过（不删消息）

### Step 4：配置（可选，先常量）

- 阈值/摘要长度先做模块常量，暂不进 config.yaml（避免扩大改动面）

### Step 5：测试

见下方测试计划。

---

## 4. 测试计划

### 4.1 单元测试（`core/memory.py`）

- `get_compress_candidates`：
  - 正常轮次（user→assistant）→ 返回该轮 id
  - 轮次含工具（user→assistant(tool_calls)→tool→assistant）→ 整轮返回
  - 末尾是 tool 消息 → 回退到该轮 assistant，不切到 tool 结果之后
  - 候选不足/无完整轮次 → 空列表
- `append_summary` / `get_summaries` 存取与顺序
- `delete_messages_by_ids` 后 `get_messages` 不再包含

### 4.2 集成测试（mock 摘要 LLM）

- 造 >24 条消息（含多轮工具调用），跑一次 `chat_stream`（复用 `eval/fakes.py` 的 ScriptedClient 思路 + 摘要调用返回固定文本的 FakeClient）
- 断言：`get_summaries()` 非空；被压缩消息已删除；`build_system_prompt()` 含"早前对话摘要"段
- **失败场景**：摘要 LLM 抛异常 → 消息一条未删，对话正常结束

### 4.3 回归

- `python test_hitl.py`（HITL 链路）
- `python eval/run_eval.py`（mock 模式 5/5 通过）
- 多会话隔离 / 可观测性相关测试不回归

### 4.4 手动验证

- 与 Agent 连续对话超过 24 条 → 观察 `/api/memory` 的 system prompt 出现摘要段；`/api/history` 早期消息消失

---

## 5. 验收清单（Done 定义）

- [ ] 超阈值会话自动压缩最早完整轮次，摘要写入 summaries 表
- [ ] 摘要注入 system prompt，模型可感知早期上下文
- [ ] 轮次边界正确：无孤儿 tool 消息、不切断工具链（API 不报错）
- [ ] 摘要 LLM 失败时安全跳过、不删消息、不阻塞对话
- [ ] 并发请求不重复触发压缩（防重入生效）
- [ ] 多会话摘要隔离
- [ ] 回归：test_hitl.py 通过、评估集 mock 5/5
- [ ] README（功能列表/特性表/Roadmap 勾选）+ CHANGELOG（[Unreleased]）更新

---

## 6. 注意事项 / 坑

1. **摘要调用也会消耗 token**：可选的加分项——把摘要调用的 token 记入 `rounds` 表（可观测性），或至少记入 `add_tokens`。注意别把它误算进"对话轮数"
2. **压缩时机与会话锁的关系**：`app.py` 的 `/api/chat` 在 `manager.lock(session)` 内跑 `chat_stream`，压缩若在同一锁内做，天然防并发；`_compressing` 标志作为第二道防线（锁在 turn_end 后可能已释放？——不会，锁覆盖整个流式周期，turn_end 在流内，锁仍持有）
3. **摘要 prompt 里 tool 结果要截断**：原始消息可能很长（read_file 返回值），拼接前截断到 ~200 字/条，控制输入 token
4. **不要压缩进行中的轮次**：候选只取"历史消息"（本次 turn_end 前已落库的），当前轮刚生成的消息在 turn_end 事件里尚未写入 memory（见 agent.py 逻辑），天然不冲突
5. **`get_messages` 的窗口逻辑保留**：摘要压缩是"加法"（补上下文），截断是"减法"（控窗口），两者并存

---

## 7. 前置阅读

- `core/memory.py`：get_messages / clear_history / _get_conn（表结构与消息存取）
- `core/context.py`：build_system_prompt（摘要注入位置）
- `core/agent.py`：chat_stream（turn_end 分支）与 __init__（client、_compressing 标志）
- `core/loop.py`：事件类型（turn_end / tool_call_start / tool_call_end），理解轮次结构
- `eval/fakes.py`：ScriptedClient / FakeStream（测试复用）

---

## 8. 参考实现片段（仅示意，非最终代码）

```python
# memory.py 新增（伪代码）
def get_compress_candidates(self, max_messages: int = 10) -> list[int]:
    rows = self._get_conn().execute(
        "SELECT id, content FROM messages WHERE session_id = ? ORDER BY id LIMIT ?",
        (self.session_id, max_messages + 1)   # 多取一条判断轮次边界
    ).fetchall()
    ids, roles = [], []
    for r in rows:
        try:
            msg = json.loads(r["content"])
        except Exception:
            continue
        ids.append(r["id"]); roles.append(msg.get("role"))
    # 规则：从 user 开始；结尾不能是 tool（若结尾是 tool，回退到该轮 assistant）
    while roles and roles[0] != "user":
        ids.pop(0); roles.pop(0)
    while roles and roles[-1] == "tool":
        ids.pop(); roles.pop()
    # 至少要有 user 开头的完整闭环才返回
    if "assistant" not in roles:
        return []
    return ids
```
